# Companion app icons

`instagram.png` and `youtube.png` are the 180×180 renditions of the current
U.S. App Store artwork returned by Apple's lookup API for app IDs `389801252`
and `544007664`. Refresh them from those canonical records when either service
changes its production icon.
